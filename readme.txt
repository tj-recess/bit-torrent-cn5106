Project can be compiled in two ways.
1. Using ant command
	ant clean 
	ant compile
2. If ant utility is not installed, just run 
	./compile.sh
	(compile.sh must be made executable, if not already, by running chmod +x compile.sh)


